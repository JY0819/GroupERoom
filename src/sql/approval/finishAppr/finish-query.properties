#selectfinishList=SELECT E.EMPID, A.APPRNO, E.EMPNAME, D.DOCNO, A.APPRYN, L.OPINION, A.APPRDAY, L.APPRDATE FROM APPR A JOIN EMP E ON(A.APPRWRITER = E.EMPID) JOIN DOC D ON(A.APPRNO = D.APPRNO) JOIN LOGOFAPPROVE L ON(A.APPRNO = L.APPRNO)  WHERE A.APPRCAN ='N' AND  A.WHETHEROFDELETE = 'N' AND A.APPRYN = 'Y'AND L.APPRSTATUS = 'Y' ORDER  BY A.APPRNO DESC
deleteFinish=UPDATE APPR SET APPRCAN = 'Y' WHERE APPRNO = ?

#update selectfinishList
selectfinishList=SELECT DISTINCT E.EMPID, A.APPRNO, E.EMPNAME, D.DOCNO, A.APPRYN, A.APPRDAY, L.APPRDATE FROM APPR A JOIN EMP E ON(A.APPRWRITER = E.EMPID) JOIN DOC D ON(A.APPRNO = D.APPRNO) JOIN LOGOFAPPROVE L ON(A.APPRNO = L.APPRNO)  WHERE A.APPRCAN ='N' AND  A.WHETHEROFDELETE = 'N' AND A.APPRYN = 'Y'AND L.APPRSTATUS = 'Y' ORDER  BY A.APPRNO DESC

#pagging
#pageselectList=SELECT RNUM, EMPID, APPRNO, EMPNAME, DOCNO, APPRYN, OPINION, APPRDAY, APPRDATE FROM(SELECT ROWNUM RNUM, EMPID, APPRNO, EMPNAME, DOCNO, APPRYN, OPINION, APPRDAY, APPRDATE FROM (SELECT E.EMPID, A.APPRNO, E.EMPNAME, D.DOCNO, A.APPRYN, L.OPINION, A.APPRDAY, L.APPRDATE FROM APPR A JOIN EMP E ON(A.APPRWRITER = E.EMPID) JOIN DOC D ON(A.APPRNO = D.APPRNO) JOIN LOGOFAPPROVE L ON(A.APPRNO = L.APPRNO) WHERE A.APPRCAN ='N' AND  A.APPRYN = 'Y' AND A.WHETHEROFDELETE = 'N' AND L.APPRSTATUS = 'Y' ORDER  BY A.APPRNO DESC)) WHERE RNUM BETWEEN ? AND ?
listCount=SELECT COUNT(*) FROM APPR WHERE APPRCAN = 'N' AND whetherofdelete = 'N'

#update paggingselectList
pageselectList=SELECT RNUM, EMPID, APPRNO, EMPNAME, DOCNO, APPRYN, APPRDAY, APPRDATE FROM(SELECT ROWNUM RNUM, EMPID, APPRNO, EMPNAME, DOCNO, APPRYN, APPRDAY, APPRDATE FROM (SELECT DISTINCT E.EMPID, A.APPRNO, E.EMPNAME, D.DOCNO, A.APPRYN, A.APPRDAY, L.APPRDATE FROM APPR A JOIN EMP E ON(A.APPRWRITER = E.EMPID) JOIN DOC D ON(A.APPRNO = D.APPRNO) JOIN LOGOFAPPROVE L ON(A.APPRNO = L.APPRNO) WHERE A.APPRCAN ='N' AND  A.APPRYN = 'Y' AND A.WHETHEROFDELETE = 'N' AND L.APPRSTATUS = 'Y' ORDER  BY A.APPRNO DESC)) WHERE RNUM BETWEEN ? AND ?